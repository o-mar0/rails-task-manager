<h1>Tasks</h1>

<% @tasks.each do |task| %>
  <%= form_for task do |t| %>
    <%= t.check_box :completed %>
    <%= link_to task.title, task_path(task)  %>
    <%= link_to 'X', task_path(task), method: :delete %>
  <% end %>
<% end %>

  <%= link_to "Add a new task", new_task_path(@task)  %>


