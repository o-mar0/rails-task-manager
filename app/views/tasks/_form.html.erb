<%= form_for(@task) do |t| %>
  <%= t.label :title, 'enter a title'%>
  <%= t.text_field :title %>

  <%= t.label :details, 'enter details'%>
  <%= t.text_field :details %>


  <%= t.check_box :completed %>

  <%= t.submit %>
<% end %>
